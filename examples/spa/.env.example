# Copy this to .env

# Sanity project (defaults are built in — only override if needed)
VITE_SANITY_PROJECT_ID=bb8k7pej
VITE_SANITY_DATASET=production

# Route environment (matches baseUrls[].name in route config)
VITE_SANITY_ROUTES_ENV=production
