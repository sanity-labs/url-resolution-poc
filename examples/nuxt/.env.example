# Copy this to .env

# Sanity read token — needed because route map shard IDs contain dots (private docs)
# Nuxt auto-maps NUXT_* env vars to runtimeConfig
NUXT_SANITY_READ_TOKEN=your-read-token-here

# Route environment (matches baseUrls[].name in route config)
NUXT_SANITY_ROUTES_ENV=production
