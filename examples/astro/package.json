{
  "name": "example-astro",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "pnpm --filter @sanity/routes build && astro build",
    "preview": "astro preview",
    "typegen": "cd ../../studio && pnpx sanity schema extract --enforce-required-fields && pnpx sanity typegen generate"
  },
  "dependencies": {
    "@sanity/astro": "^3.2.0",
    "@sanity/client": "^7.16.0",
    "@sanity/routes": "workspace:*",
    "astro": "^5.0.0",
    "astro-portabletext": "^0.11.0",
    "groq": "^3.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
