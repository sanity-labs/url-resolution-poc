{
  "name": "url-resolution-poc",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "build:routes": "pnpm --filter @sanity/routes build",
    "clean": "rm -rf packages/routes/dist studio/.sanity examples/nextjs/.next examples/nextjs/.build && echo 'Cleaned build artifacts'",
    "rebuild": "pnpm clean && pnpm build && pnpm build:functions",
    "build:functions": "cp packages/routes/dist/handler.js studio/functions/route-sync-web/_handler.js"
  },
  "packageManager": "pnpm@10.10.0",
  "engines": {
    "node": ">=18",
    "pnpm": ">=10"
  },
  "dependencies": {
    "@sanity/client": "^7.16.0"
  }
}